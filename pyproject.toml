[project]
name = "polaris-antiviral-potency"
version = "0.1.0"
description = "Rigorous, reproducible ML benchmark on the Polaris ASAP Antiviral Potency task (chronological split, leakage-free baseline)."
requires-python = ">=3.12,<3.13"
dependencies = [
    "polaris-lib==0.13.0",
    "rdkit==2026.3.6",
    "scikit-learn==1.9.1",
    "pandas==3.0.5",
    "numpy==2.5.3",
    "matplotlib==3.11.2",
]

[tool.uv]
package = false
